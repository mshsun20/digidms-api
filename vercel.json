{
    "version": 2,
    "builds": [
        {
            "src": "api/[...path].js",
            "use": "@vercel/node"
        }
    ],
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/[...path].js"
        }
    ]
}